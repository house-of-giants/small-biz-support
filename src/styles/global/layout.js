import styled from 'styled-components'
import { container, sp } from '../base/variables'

export const StyledContainer = styled.div`
	margin: 0 auto;
	max-width: ${container.width};
	padding: ${sp.xl} ${sp.base};
`
